{
  "name": "ld32",
  "version": "0.1.0",
  "description": "Ludum Dare 32",
  "main": "game.js",
  "engines": {
    "node": "0.12.x"
  },
  "scripts": {
    "postinstall": "coffee --compile --map *.coffee",
    "start": "http-server -p ${PORT-8080} -c-1",
    "watch": "http-server -p ${PORT-8080} & coffee --compile --map --watch *.coffee",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/noonat/ld32.git"
  },
  "author": "Nathan Ostgard",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/noonat/ld32/issues"
  },
  "homepage": "https://github.com/noonat/ld32",
  "dependencies": {
    "coffee-script": "^1.9.2",
    "http-server": "^0.8.0"
  }
}
